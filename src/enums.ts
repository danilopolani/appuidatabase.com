export enum AppCategory {
  Messaging = 'Messaging',
}

export enum ScreenCategory {
  Modals = 'modals',
  Notifications = 'notifications',
  Navigation = 'navigation',
  Popover = 'popover',
}

export enum Platform {
  Mac = 'Mac',
  Windows = 'Windows',
  Linux = 'Linux',
  IPad = 'iPad',
  IPhone = 'iPhone',
  Android = 'Android',

  // Generic values if app is the same on all the platforms
  Desktop = 'Desktop',
  Mobile = 'Mobile',
}
